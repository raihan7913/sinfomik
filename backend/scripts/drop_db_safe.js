// DROP DB helper disabled per user request.
// File kept for traceability but intentionally inert to avoid accidental destructive operations.
console.log('drop_db_safe.js is disabled.');
process.exit(0);
